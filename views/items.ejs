<html>
    <body>
        <h1>Items in category</h1>
        <ul>
            <% items.forEach(item => { %>
                <li>ID: <%= item.id %> Name: <%= item.name %> Quantity: <%= item.quantity %> Price: <%= item.price %></li>
            <% }) %>
        </ul>
    </body>
</html>